import { Component } from '@angular/core';

/**
 * Component that renders the grid's header
 */
@Component({
  selector: 'is-grid-header',
  template: '<ng-content></ng-content>'
})
export class IsGridHeader {}
