import { Component } from '@angular/core';

/**
 * Component that renders the grid's footer
 */
@Component({
  selector: 'is-grid-footer',
  template: '<ng-content></ng-content>'
})
export class IsGridFooter {}
