import { InjectionToken } from '@angular/core';
import { IsTableConfiguration } from './table-configuration';

/**
 * Injection token for table configuration
 */
export const IS_TABLE_CONFIGURATION = new InjectionToken<IsTableConfiguration>('istable.configuration');
