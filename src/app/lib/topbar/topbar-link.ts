/**
 * Interface that defines topbar links
 */
export class IsTopbarLink {
  /**
   * The link used on the <a> tag
   */
  link: string;
  /**
   * The label being displayed on the <a> tag
   */
  label: string;
  /**
   * The icon next to the label <a> tag
   */
  icon: string;
}
